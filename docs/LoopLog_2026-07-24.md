# Cleanup Loop Log — 2026-07-24

Branch: `feat/loop-cleanup-2026-07-24` (from main @ 6179847). Checkpoint commits use `--no-gpg-sign` per explicit authorization. Never pushed, never merged.

---

## Task 1 — TODO.md refresh

**Spec.** Docs-only. Sync `TODO.md` with shipped state:
- Bump "Last updated" to 2026-07-24 (and §0 header date).
- §0 table: add/update rows for shipped features — body keyways (+180° toggle, auto-body "make editable", merged 89c49d0), runout bubble editor (TIR value + high-spot, merged 92461ba), spooned keyways (10dff2a), diameter callouts below-shaft (313b328), dimension value seated in a break (5c0fc58), spooned-KW footer note (d144d7f).
- §3 Shelved: remove "Body keyway support — shelved" (it shipped).
- §7 Non-Goals: remove "Body keyway (shelved)".
- §2 checklist untouched (tasks 2–3 of this loop will check those off as they land).
- Verify claims against CHANGELOG.md / git log; no invariants touched.

**Test plan.** Docs-only; full-suite baseline run (started before task) must be green.

**Result.** Done. Files changed: `TODO.md`, `CHANGELOG.md`. Baseline `:app:testDebugUnitTest` green (exit 0) on branch point 6179847. §0 gained rows for body keyways / runout bubble editor / spooned keyways / diameter callouts / dim-value-in-break; Shelved subsection and body-keyway non-goal removed. Subagent cross-checked dates against git (89c49d0, 92461ba, 10dff2a-era commits, 313b328, 5c0fc58, d144d7f) — no discrepancies.

**Commit:** 96f0e72

---

## Task 2 — Validation §2.1 remainders

**Spec.**
- **(A) Taper slope validated only when `lengthMm > 0`.** Investigation: the guards largely already exist — `autoTaperRate` returns null at `lengthMm <= 0` (`TaperRateAuto.kt:50`), `manualTaperRateWarning` requires `lengthMm > 0` (`:172`), `manualTaperRateBlockingMessage`'s derive-prompt requires `lengthMm > 0` (`:145`), and `ShaftViewModel.deriveTaperDiameters` early-returns at `lengthMm <= 0` (`ShaftViewModel.kt:2279`). Work: lock the contract in with explicit unit tests at `lengthMm == 0` (and negative) across those four surfaces; fix any gap the tests expose. Syntax-only messages (unparseable text, ambiguous "1") may still fire at zero length — they are not slope validation.
- **(B) Free-to-End badge uses safeSpec at `overallLengthMm == 0`.** `FreeToEndBadge` (`ShaftScreen.kt:1422`) computes `freeSignedMm = spec.overallLengthMm - lastOccupiedEndMm(spec)`; at OAL==0 that yields a phantom red "oversized" badge while the preview draws with `safeSpec` (OAL extended to last occupied end, `ShaftDrawing.kt:180`). Fix: pure helper (new small file under `ui/util/`, unit-testable) computing the signed free value with effective OAL = `lastOccupiedEndMm` when `overallLengthMm == 0f` and parts exist; badge calls it. Suppression rules (manual-mode-only, precision-component gate, oversized-always-red) unchanged.
- Docs: `FreeToEndBadge.md` → v1.3 (document safeSpec behavior; correct the stale "Hidden when overallLengthMm <= 0" claim to actual behavior). `docs/VALIDATION_RULES.md` untouched except if §3.3 wording needs the slope-guard note. TODO §2.1 both boxes checked; §3 duplicate "`freeToEndMm` safeSpec" backlog entry removed.

**Test plan.** New unit tests: TaperRateAutoTest additions (zero/negative length inertness), new FreeToEnd badge-math test class (OAL==0 with components → 0; OAL==0 no components → 0; normal OAL positive/oversized unchanged). Full suite must stay green.

**Invariants touched.** Free-to-End badge suppression (CLAUDE.md) — value computation changes only at OAL==0; suppression conditions untouched. 3% taper tolerance untouched.

**Result.** Done. Item A: no production gap — guards confirmed; 6 pinning tests added to `TaperRateAutoTest.kt` + 1 to `TaperRateTest.kt`. Item B: new `ui/util/FreeToEndBadgeMath.kt` (`freeToEndSignedMm`), badge in `ShaftScreen.kt` now uses it; new `FreeToEndBadgeMathTest.kt` (5 cases). Docs: `FreeToEndBadge.md` v1.3, `VALIDATION_RULES.md` §3.3 note, TODO §2.1 checked + §3 duplicate removed, CHANGELOG entry. Full suite `:app:testDebugUnitTest` BUILD SUCCESSFUL.

**Commit:** ffdf92b

---

## Task 3 — Validation §2.2 warning rules (VALIDATION_RULES.md §3–4)

**Spec.** Non-blocking warnings only; `blockingExportError()` stays the single export gate (no changes near it). All pure math in `ui/util/ComponentWarnings.kt` (+ new spec-level helper), unit-tested.

- **§3.2 body↔body discontinuity:** bodies whose spans abut within `ADJACENCY_EPS_MM = 0.5` and both dias > 0 warn when the step ratio (max/min dia) exceeds `1.5×`. Surfaced on both bodies' carousel cards.
- **§3.3 taper vs adjacent body:** at a shared face (taper start/end abutting a body end/start within the same eps), warn when the taper's face diameter differs from the body dia by > 10% relative (both > 0). Surfaced on the taper card.
- **§3.5 liner OD < underlying body dia:** for any stored body overlapping the liner's span, warn when `liner.odMm < body.diaMm − 0.001` (both > 0). Surfaced on the liner card.
- **§4.3 spec tiny segments:** spec-level message when any component length is in (0, 1 mm) — count included. Component-level flags already exist and stay.
- **§4.3 zero-body coverage:** warn when `bodies.isEmpty()` and at least one taper/thread/liner exists (empty drafts stay silent).
- **Surfacing decision:** component-scoped rules join the existing carousel `warningMessage` slot (multiple messages joined with "; "). The two **spec-level** warnings are computed + tested (`specWarningMessages(spec)`) but **not wired to UI** — there is no spec-level warning surface today and choosing one (badge? banner?) is a UX decision for Chris. Logged as deferred.
- Thresholds (1.5×, 10%, 0.5 mm eps) are my documented defaults as named constants — flagged for Chris's review; trivially tunable.

**Test plan.** New unit test class(es) for all five rules: trigger / no-trigger / boundary / zero-dia sentinel skip / adjacency eps. Existing BlockingExportErrorTest continues to pin the export gate.

**Invariants touched.** None weakened: warnings never block; bodies stay out of `collidingIds()`; reference features (bolt slots, pits, runout) not consulted by any new rule.

**Result.** Done. `ComponentWarnings.kt` rewritten with named constants + five rules; new signatures `bodyWarningMessages`/`taperWarningMessages`/`linerWarningMessages` (List<String>, joined "; " on cards) + `specWarningMessages` (computed only, no UI surface yet); `threadWarningMessage` unchanged. Carousel call sites updated; 3 dead imports removed from `ShaftScreen.kt`. 24 new tests in `ComponentWarningsTest.kt`; `BlockingExportErrorTest` untouched/green; `blockingExportError()` untouched. Docs: VALIDATION_RULES §3.2/§3.3/§3.5/§4.3 flipped to implemented, TODO §2.2 checked, CHANGELOG entry. Full suite BUILD SUCCESSFUL.

**Deferred (UX decision for Chris):** where to surface the two spec-level warnings (`specWarningMessages`) — no spec-level warning UI exists today. Also: thresholds 1.5× body step / 10% taper mismatch / 0.5 mm adjacency eps are my engineering defaults — review welcome.

**Commit:** _(next)_
