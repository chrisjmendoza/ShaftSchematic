package com.android.shaftschematic.model

/**
 * Authoring reference for components that can be measured from AFT or FWD.
 * This is metadata only and must never mutate physical geometry.
 */
enum class AuthoredReference { AFT, FWD }
